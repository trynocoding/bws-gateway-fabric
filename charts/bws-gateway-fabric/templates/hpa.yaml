{{- if and (eq .Values.bwsGateway.kind "deployment") .Values.bwsGateway.autoscaling.enable -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  {{- with .Values.bwsGateway.autoscaling.annotations }}
  annotations: {{ toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "bws-gateway.labels" . | nindent 4 }}
    {{- with .Values.bwsGateway.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  name: {{ include "bws-gateway.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "bws-gateway.fullname" . }}
  {{- if .Values.bwsGateway.autoscaling.minReplicas }}
  minReplicas: {{ .Values.bwsGateway.autoscaling.minReplicas }}
  {{- end }}
  maxReplicas: {{ .Values.bwsGateway.autoscaling.maxReplicas }}
  metrics:
  {{- with .Values.bwsGateway.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ . }}
  {{- end }}
  {{- with .Values.bwsGateway.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ . }}
  {{- end }}
  {{- with .Values.bwsGateway.autoscaling.metrics }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.bwsGateway.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
