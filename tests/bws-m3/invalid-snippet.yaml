apiVersion: gateway.nginx.org/v1alpha1
kind: SnippetsPolicy
metadata:
  name: invalid-bws-config
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: bws-gateway
  snippets:
    - context: http
      value: "gzip deliberately-invalid-value;"
