apiVersion: gateway.bessystem.com/v1alpha1
kind: AuthenticationFilter
metadata:
  name: jwt-remote-auth
spec:
  type: JWT
  jwt:
    realm: "JWT Remote Authentication"
    source: Remote
    remote:
      uri: https://keycloak.authentication-filter.svc.cluster.local:8443/realms/nginx-gateway/protocol/openid-connect/certs
      caCertificateRefs:
        - name: keycloak-ca-secret
    keyCache: 10m
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jwt-remote-cafe-routes
spec:
  parentRefs:
    - name: auth-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /jwt-remote-coffee
      backendRefs:
        - name: coffee
          port: 80
      filters:
        - type: ExtensionRef
          extensionRef:
            group: gateway.bessystem.com
            kind: AuthenticationFilter
            name: jwt-remote-auth
    - matches:
        - path:
            type: PathPrefix
            value: /jwt-remote-tea
      backendRefs:
        - name: tea
          port: 80
